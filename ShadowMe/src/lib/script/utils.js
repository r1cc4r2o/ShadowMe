/**
 * Transforms radians into degrees
 * @param {float} radians - The angle in radians
 * @returns The angle converted into degrees
 */
export function radians_to_degrees(radians) {
    return radians * (180 / Math.PI);
}